[
  {
    "name": "/subscribe",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Subscriptions aren’t set up yet. Please try again when persistent storage is available."
            }
          }
        ],
        "send": {
          "text": "/subscribe"
        }
      }
    ]
  }
  ,{
    "name": "Subscribe menu button opens the flow",
    "steps": [{ "send": { "callback": "subscribe:open" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Subscriptions aren’t set up yet. Please try again when persistent storage is available." } }] }]
  }
]
