[
  {
    "name": "/match",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Send the match ID you want to study."
            }
          }
        ],
        "send": {
          "text": "/match"
        }
      }
    ]
  }
  ,{
    "name": "Match menu button requests an ID",
    "steps": [{ "send": { "callback": "match:open" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Send the match ID you want to study." } }] }]
  }
]
